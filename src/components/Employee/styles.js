import styled from 'styled-components'

export const Container = styled.div`
    .employee{
      transform: scaleX(-1);
      height: 120px;
      position: absolute;
      display: block;
      bottom: 0px;
      right: 35%;
      
    }
`